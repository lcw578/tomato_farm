# 番茄采摘 VLA 项目的 Gazebo 端口隔离约定
# 背景：本机另一个项目 (HEIGHT_CAG_Integrated) 使用默认端口 11345 且会周期性
# 重启仿真并清理所有 gzserver。本项目所有 Gazebo 实例统一使用 11390 端口，
# 与其完全隔离。用法：source gazebo_port.env 后再启动任何 gzserver/gzclient/roslaunch。
export GAZEBO_MASTER_URI=http://localhost:11390
export GAZEBO_MODEL_DATABASE_URI=""
